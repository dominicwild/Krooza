package menu;

/**
 * An enum representing the type of menu. Used for easy switching between menus
 * as a passed parameter in MenuHandler - Dominic Wild
 */
public enum MenuType {

    MAIN, OPTIONS, PAUSE, GARAGE, VENDOR, MODE, GAME_OVER, HORDE_GAME_OVER;
}
